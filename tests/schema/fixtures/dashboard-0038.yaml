dashboard:
  title: New dashboard
  templating:
    - name: myqueue
      type: query
      datasource:
        uid: logistics-inhouse-prom
      query: label_values(metric{label=~".*"}, label)
      includeAll: true
      multi: true
